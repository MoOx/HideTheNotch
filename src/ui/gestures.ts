/**
 * How far the finger travels for the full range of the main setting.
 *
 * Shared rather than kept next to the gesture because the demo draws a finger
 * moving: deriving that movement from the same number is what stops the demo
 * from showing a gesture the app does not have.
 */
export const PARAM_TRAVEL = 300;
